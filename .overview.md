# 一键启动脚本 + 日志/进度可见性修复

## 完成内容

### A. 一键启动脚本 `start.sh`
- 路径: `/Users/lcy/video/vimax-agnes/start.sh`
- 自动设置 `AGNES_API_KEY` 环境变量（已内置）
- 无参数运行: 显示可用创意列表
- 带参数运行: `./start.sh singing_dancing` 一键启动完整 pipeline
- 自动检查虚拟环境是否就绪

### B. 日志系统
**`run_creative.py`** — main() 开头添加 logging 配置
- 控制台输出 → stderr
- 文件输出 → `.working_dir/logs/run_YYYYMMDD_HHMMSS.log`
- 格式: `HH:MM:SS [LEVEL] name: message`

**`main_idea2video.py`** — 同样添加 FileHandler

### C. 进度可见性
**`agents/screenwriter.py`** — 关键方法添加中文进度输出:
- `develop_story()`: 📖 正在生成故事... → ✅ 故事生成完成
- `write_script()`: 📝 正在编写脚本... → ✅ 脚本完成，共 X 个场景
- `generate_end_frame_prompts()`: 🎬 正在生成关键帧提示词... → ✅ 关键帧提示词完成，共 X 个

**`tools/video_generator_agnes_api.py`** — `_poll_task` 每 3 次轮询输出:
- ⏳ 视频生成中... {status} {progress}%

**`tools/image_generator_agnes_api.py`** — `generate_single_image()`:
- 🖼️ 正在生成图片... → ✅ 图片生成完成

所有 `print()` 均使用 `flush=True` 确保实时输出。

## 使用方式

```bash
./start.sh                    # 查看可用创意
./start.sh singing_dancing    # 启动完整生成流程
```

## 验证通过
- 日志文件已生成: `.working_dir/logs/run_20260610_184219.log`
- 创意列表正常显示（example_creative, hot_spring_robot, singing_dancing）
